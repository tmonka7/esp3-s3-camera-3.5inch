build/
sdkconfig
sdkconfig.old
*.pyc
.vscode/

# The component manager is not used -- every dependency is vendored under
# third_party/ so the project builds offline. These would only appear if
# someone re-introduced an idf_component.yml.
managed_components/
dependencies.lock
